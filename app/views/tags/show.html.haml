%h1= @tag.name
- if @tag.books.empty?
  %h2 Статей нет
- else
  - @tag.books.each do |book|
    %p= link_to book.title, book_path(book)
    %p= envelope_for(book, size: 80)
    %p= raw book.tags.map(&:name).map{ |t| link_to t, tag_path(t), class: "badge badge-primary"}.join(' ')
    %hr/
